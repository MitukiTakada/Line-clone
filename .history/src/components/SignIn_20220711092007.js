import { Button } from '@material-ui/core'
import React from 'react'

const SignIn = () => {
  return (
    <div>
      <Button onClick={}>Googleでログインする</Button>
    </div>
  )
}

export default SignIn
